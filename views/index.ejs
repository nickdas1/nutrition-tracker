<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nutrition Tracker</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>

<body>
    <div class="container">
        <div class="text-center mb-3">
            <h1>Nutrition Tracker</h1>
            <h3>Total Calories: <strong><span id="totalCalories">0</span></strong>
                Total Protein: <strong><span id="totalProtein">0</span></strong>
            </h3>
        </div>

        <form id="searchForm" class="d-flex justify-content-center mb-4">
            <label for="search" class="form-label">Enter a Food</label>
            <input type="text" id="search" placeholder="Search For a Food" name="query" class="form-control">
            <button class="btn btn-primary">Search</button>
        </form>

        <div id="addOtherFood" class="input-group d-flex justify-content-center mb-4">
            <span class="input-group-text">Add your own:</span>
            <input id="foodName" class="form-control" type="text" placeholder="Enter Name of Food">
            <input id="otherCalories" class="form-control" type="number" placeholder="Enter Calories">
            <input id="otherProtein" class="form-control" type="number" placeholder="Enter Protein (g)">
            <button id="otherButton" class="btn btn-success">Add</button>
        </div>

        <div id="searchResults">
            <p>Search Results:</p>
            <button id="clear" class="btn btn-danger mb-2">Clear Results</button>
        </div>
        <div id="results" class="mb-5">
        </div>

        <div id="selectedFood">
            <p>Added Food:</p>
        </div>
    </div>

    <script src="/javascripts/index.js"></script>

</body>

</html>